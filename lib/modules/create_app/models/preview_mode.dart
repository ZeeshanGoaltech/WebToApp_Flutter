enum PreviewMode { splash, onboarding, navigation, webview }

enum BuildFormat { apk, aab }

enum BuildState { ready, building, failed, success, downloading, downloaded }

enum SigningMode { auto, upload }
